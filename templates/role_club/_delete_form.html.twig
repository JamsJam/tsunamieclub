<form method="post" action="{{ path('app_role_club_delete', {'id': role_club.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role_club.id) }}">
    <button class="btn">Delete</button>
</form>
