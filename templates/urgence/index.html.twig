{% extends 'base.html.twig' %}

{% block title %}Urgence index{% endblock %}

{% block body %}
    <h1>Urgence index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Lien</th>
                <th>Telephone</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for urgence in urgences %}
            <tr>
                <td>{{ urgence.id }}</td>
                <td>{{ urgence.nom }}</td>
                <td>{{ urgence.prenom }}</td>
                <td>{{ urgence.lien }}</td>
                <td>{{ urgence.telephone }}</td>
                <td>
                    <a href="{{ path('app_urgence_show', {'id': urgence.id}) }}">show</a>
                    <a href="{{ path('app_urgence_edit', {'id': urgence.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_urgence_new') }}">Create new</a>
{% endblock %}
