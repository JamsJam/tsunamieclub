{% extends 'base.html.twig' %}

{% block title %}{{parent()}}Modifier{% endblock %}

{% block body %}

<div class="contactForm">
    <h1 class="title title--h1">Modifier les informations</h1>
    <a href="{{ path('app_contact_index') }}" class="bouton bouton--retour">Retour</a>

    {{ include('contact/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('contact/_delete_form.html.twig') }}
</div>

{% endblock %}
